"""
Operadores aritmeticos
    Suma -> +
    Resta -> -
    Multiplicacion -> *
    Division -> /
    Residuo -> %
    Potencis -> **

Operadores logicos
    AND -> and
    OR -> or
    NOT -> not

Operadores relacionales
    Mayor que -> >
    Menor que -> <
    Mayor o igual que -> >=
    Menor o igual que -> <=
    Diferente -> !=
    Igual -> ==

Conversion de tipos de datos
    Entero, Decimal y Booleano a String -> str()
    Entero Y String a Float -> float()
    String y Float a Entero -> int()
    Entero, Decimal Y String a Booloeano -> bool()

Declaracion de ciclos
    # Ciclo for
    for contador in range(0, 5):
        print(contador)

    # Ciclo while
    numero = 5
    while numero >= 0:
        print(numero)
        numero -= 1

Declarar un arreglo
    frutas = {'Fresa':'roja', 'Limon':'verde', 'Papaya':'naranja', 'Manzana':'amarilla', 'Guayaba':'rosa'}

    # len() Mide el tamaño del arreglo
    print("\nLongitud del arreglo: ", len(frutas), "\n")

    # Imprimir un arreglo
    for nombre, color in frutas.items():
        print(nombre, "es de color", color)

    for nombre in frutas:
        print(nombre, 'es de color', frutas[nombre])

Declarar una variable tpo Boolean
    # Para declara una variable de tipo boolean se tiene que declarar con la primera letra mayuscula
    variablebool = True     # False
    print(variablebool, type(variablebool))

Funcion floor -> //


Formas de Imprimir texto (No tengo definido el nombre de estas funciones)
 \" – double quote
 \\ – single backslash
 \a – bell/alert
 \b – backspace
 \r – carriage return
 \n – newline
 \t – tab

"""
